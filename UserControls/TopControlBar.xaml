<UserControl x:Class="DicomViewer.UserControls.TopControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DicomViewer.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Azure">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Name="FileExplorer" Background="Black" Command="{Binding OpenNewFile}" ClickMode="Release" Width="80" HorizontalAlignment="Left">
            <Image Source="/Assets/NeatFolder.webp" HorizontalAlignment="Center"/>
        </Button>



    </Grid>
</UserControl>
